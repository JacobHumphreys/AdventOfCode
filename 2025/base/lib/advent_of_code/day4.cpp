#include "advent_of_code.h"
#include "files.h"
#include "string"

using std::string;

int day4::run(string filepath, int part) {
    auto lines = readFileToVec(filepath);

    if (part == 2) {} 
    return 0;
}
